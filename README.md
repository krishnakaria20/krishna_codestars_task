# Codestars Dashboard — Task 2 (Tier 2 Advanced)

Full README lands in Stage 6.

```bash
# terminal 1
cd server && npm install && npm run dev

# terminal 2
cd client && npm install && npm run dev
```

Client: http://localhost:5173
Server: http://localhost:5000
