# CP Pulse Client

React/Vite frontend for the CP Pulse multi-platform competitive-programming analytics dashboard.

## Local

```bash
npm install
npm run dev
```

For a separate production backend, set `VITE_API_URL` to the backend API base, for example `https://your-backend.example.com/api`.
